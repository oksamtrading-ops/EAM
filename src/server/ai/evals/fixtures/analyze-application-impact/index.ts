import { CRITICAL_UNCOVERED } from "./critical-uncovered";
import { SAFE_REDUNDANT } from "./safe-redundant";
import { PARTIAL_COVERAGE } from "./partial-coverage";

export const IMPACT_ANALYSIS_FIXTURES = [
  CRITICAL_UNCOVERED,
  SAFE_REDUNDANT,
  PARTIAL_COVERAGE,
];
